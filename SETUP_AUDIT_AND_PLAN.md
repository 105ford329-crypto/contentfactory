# 🔍 Content Factory - Аудит и План Настройки

**Дата:** 2026-01-15
**Статус:** Требуется полная переделка архитектуры

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. **AI Orchestrator НЕ МОЖЕТ вызывать workflows**

**Проблема:**
- AI Orchestrator (Workflow 0) использует 11 `toolWorkflow` nodes
- **НИ ОДИН** tool не имеет определённых `workflowInputs`
- Все вызываемые workflows используют **Schedule** или **Webhook** триггеры
- НИ ОДИН workflow не имеет `executeWorkflowTrigger`

**Последствия:**
- ❌ AI агент не может вызвать workflows через tool calls
- ❌ Telegram команды не будут работать
- ❌ Вся оркестрация через Telegram Bot невозможна

**Решение:**
Каждый workflow должен иметь:
1. `executeWorkflowTrigger` node
2. Определённые `workflowInputs` в формате:
```json
{
  "values": [
    {"name": "username", "type": "string"},
    {"name": "chat_id", "type": "string"}
  ]
}
```
3. Tool в Orchestrator должен иметь mappedParameters

---

### 2. **Отсутствуют Environment Variables**

**Проблема:**
- Все парсеры используют `$env.GOOGLE_SHEET_ID` - **не определён**
- Нет `$env.TELEGRAM_CHAT_ID`
- Возможно используются другие env vars, но не документированы

**Список обязательных ENV переменных:**

```bash
# Google Sheets
GOOGLE_SHEET_ID=<ID главной таблицы>

# Telegram
TELEGRAM_CHAT_ID=<Default chat ID>
TELEGRAM_BOT_TOKEN=<Токен бота>

# OpenRouter (если используется)
OPENROUTER_REFERER=https://n8n.io

# API Keys (через credentials, но могут быть в env)
# SCRAPECREATORS_API_KEY=xxx
# OPENAI_API_KEY=xxx
# ANTHROPIC_API_KEY=xxx
# HEYGEN_API_KEY=xxx
# IDEOGRAM_API_KEY=xxx
```

---

### 3. **Несовместимость форматов данных**

#### 3.1 Парсеры (WF 1, 2, 4, 8)

**Выход парсеров:**
```javascript
{
  id: "video_id",
  competitor_username: "username",
  platform: "tiktok|instagram|youtube|pinterest",
  url: "https://...",
  views: 12345,
  likes: 567,
  comments: 89,
  shares: 12,
  vfr: "1.23",
  er: "4.56",
  virality: "VIRUS|AVERAGE|LOW",
  hook: "First line of description",
  description: "Full text",
  hashtags: "#tag1, #tag2",
  duration: 30,
  created_at: "2025-12-24T...",
  chat_id: "123456"
}
```

**Проблема:**
- VFR и ER возвращаются как **string** ("1.23"), но для аналитики нужны **numbers**
- `chat_id` может отсутствовать при Schedule trigger
- Нет единого формата для `musicInfo` (TikTok vs Instagram)

#### 3.2 Video Montage (WF 3)

**Ожидает на входе:**
```javascript
{
  content_ids: ["id1", "id2", "id3"]  // array
}
```

**Проблема:**
- Откуда брать эти IDs?
- Нет механизма выбора топ контента
- Нет связи с парсерами

#### 3.3 Carousel Generator (WF 5)

**Ожидает:**
```javascript
{
  topic: "AI лайфхаки"  // string
}
```

**Вопросы:**
- Где генерируется topic?
- Нет связи с трендами из парсеров

#### 3.4 Threads Publisher (WF 6)

**Ожидает:**
```javascript
{
  content_type: "carousel|video|text",
  content_data: {...}  // структура не определена
}
```

**Проблема:**
- Непонятный формат `content_data`
- Нет спецификации для разных `content_type`

#### 3.5 Statistics (WF 9)

**Ожидает:**
```javascript
{
  period: "week|month|all"
}
```

**Проблема:**
- Нет листа "API_Costs" в Google Sheets
- Структура не определена

#### 3.6 Vector Search (WF 11)

**Ожидает:**
```javascript
{
  query: "найди похожие примеры про AI"
}
```

**Проблема:**
- Supabase credentials не настроены
- Таблица `documents` не создана
- Нет начальной индексации

---

## 📊 АУДИТ ТРИГГЕРОВ

### Schedule Triggers (Работают автономно)

| Workflow | Schedule | Назначение |
|----------|----------|------------|
| WF 1 - TikTok Parser | Каждые 6 часов | Автоматический парсинг |
| WF 2 - Instagram Parser | Каждые 6 часов | Автоматический парсинг |
| WF 4 - YouTube Parser | Каждые 12 часов | Автоматический парсинг |
| WF 7 - YouTube Deep | Каждые 3 дня в 23:00 | Глубокий анализ |
| WF 8 - Pinterest | Каждые 8 часов | Автоматический парсинг |
| WF 6 - Threads Publisher | Каждые 4 часа | Автопубликация |
| WF 10 - Analytics | Каждые 6 часов | Сбор аналитики |
| WF 11 - Vector Learning | Ежедневно в 00:00 | Индексация |

**Вывод:** Парсеры могут работать **независимо** от Orchestrator

### Без триггеров (Требуют добавления executeWorkflowTrigger)

| Workflow | Текущее состояние |
|----------|-------------------|
| WF 3 - Video Montage | ❌ Нет триггера |
| WF 5 - Carousel Generator | ❌ Нет триггера |
| WF 9 - Cost Tracker | ❌ Нет триггера |
| WF 12 - Pinterest Publisher | ❌ Нет триггера |

---

## 🗄️ GOOGLE SHEETS - Структура данных

### Требуемые листы:

#### 1. **Competitors** (Список конкурентов)
```
id | username | platform | status | last_parsed | followers | added_date
```

Пример:
```
1 | viral_guy | tiktok | active | 2025-12-24 12:00:00 | 125000 | 2025-12-01
2 | trendy_girl | instagram | active | 2025-12-24 13:00:00 | 89000 | 2025-12-01
```

#### 2. **Parsed_Videos** (Спарсенные видео)
```
id | competitor_id | competitor_username | platform | url | views | likes | comments | shares | vfr | er | share_rate | virality | hook | description | hashtags | duration | thumbnail_url | created_at | parsed_at | chat_id
```

#### 3. **API_Costs** (Расходы на API)
```
timestamp | service | operation | cost | details
```

Пример:
```
2025-12-24 12:00:00 | scraper_creators | tiktok_parse | 0.03 | {"username": "viral", "videos": 30}
2025-12-24 12:05:00 | openai | analysis | 0.002 | {"model": "gpt-4o-mini", "tokens": 2000}
```

#### 4. **Published_Content** (Опубликованный контент)
```
id | platform | content_type | url | published_at | views | likes | comments | shares | performance_score
```

---

## 🔑 CREDENTIALS AUDIT

### Обязательные credentials:

#### 1. **ScraperCreators API** (`fZGnjUIKCVNKz54C`)
- Используется в: WF 1, 2
- Тип: HTTP Header Auth
- Header: `X-API-KEY`
- Стоимость: $0.03/запрос

#### 2. **OpenAI API** (несколько аккаунтов)
- `BPj3GH3bI2f4q6aI` - OpenAi account 4
- `brRphRFzNpNLW9kw` - 03.10.25
- Используется в: WF 11 (embeddings), анализ контента

#### 3. **Anthropic API (Claude)**
- Model: `claude-3-5-sonnet-20241022`
- Используется в: WF 0 (Orchestrator), WF 3, WF 11

#### 4. **Google Sheets OAuth2** (`VYgLH1nyxvDHNqbF`)
- Используется во всех парсерах
- Scope: spreadsheets

#### 5. **Supabase API** (`XavzftuBevmNShcu`)
- Используется в: WF 11 (Vector Store)
- Таблица: `documents`

#### 6. **Telegram Bot**
- Webhook ID в WF 0: `ai-orchestrator`
- Используется для отправки уведомлений

### Отсутствующие credentials:

- ❌ **HeyGen API** (для WF 3 - Video Montage)
- ❌ **Ideogram API** (для WF 3, 5)
- ❌ **Threads API** (для WF 6)
- ❌ **Pinterest API** (для WF 12)
- ❌ **YouTube API** (для WF 4, 7) - возможно через ScraperCreators

---

## 🔗 ЗАВИСИМОСТИ МЕЖДУ WORKFLOWS

```
Парсеры (1,2,4,8)
  ↓ [сохраняют в Google Sheets]
  ↓
Google Sheets "Parsed_Videos"
  ↓
  ├─→ WF 11 (Vector Learning) - индексирует контент
  │     ↓ [Supabase Vector Store]
  │     ↓
  │   WF 11 Search - поиск похожих
  │
  ├─→ WF 10 (Analytics) - анализ метрик
  │
  └─→ WF 3 (Video Montage) - выбор топ контента (НЕ РЕАЛИЗОВАНО!)
        ↓
      WF 6 (Threads Publisher)
```

**Проблема:** Нет автоматического механизма выбора топ контента для создания видео/каруселей!

---

## ⚠️ СЛАБЫЕ МЕСТА СИСТЕМЫ

### 1. **Архитектурные**

❌ **AI Orchestrator не работает** - требует полной переделки tool definitions
❌ **Нет executeWorkflowTrigger** в вызываемых workflows
❌ **Отсутствует механизм выбора топ контента** для генерации
❌ **Нет связи между парсингом и генерацией** контента

### 2. **Данные**

❌ **Метрики как strings** (vfr, er) вместо numbers
❌ **Несовместимые форматы** между workflows
❌ **Отсутствует Google Sheets** со структурой
❌ **Нет Supabase таблицы** documents

### 3. **Credentials**

❌ **50% credentials отсутствуют** (HeyGen, Ideogram, Threads, Pinterest)
❌ **Environment variables не определены**
❌ **Telegram Bot не настроен**

### 4. **Функциональность**

❌ **Video Montage** - нет механизма выбора контента
❌ **Carousel Generator** - нет источника топиков
❌ **Threads Publisher** - не определён формат данных
❌ **Cost Tracker** - нет листа API_Costs

### 5. **Мониторинг**

❌ **Нет error handling** в большинстве workflows
❌ **Нет retry логики** для критических операций
❌ **Нет алертов** при падении
❌ **Нет health checks**

---

## 📋 ПОШАГОВЫЙ ПЛАН НАСТРОЙКИ

### Этап 1: Базовая инфраструктура (Критически важно)

#### 1.1 Google Sheets Setup
```bash
Приоритет: 🔴 КРИТИЧЕСКИЙ
Время: 1 час
```

**Действия:**
1. Создать Google Spreadsheet
2. Создать листы: `Competitors`, `Parsed_Videos`, `API_Costs`, `Published_Content`
3. Добавить заголовки столбцов (см. структуру выше)
4. Скопировать Sheet ID в Environment Variables
5. Дать доступ Google Sheets OAuth2 credential

**Результат:** `$env.GOOGLE_SHEET_ID` определён

---

#### 1.2 Environment Variables
```bash
Приоритет: 🔴 КРИТИЧЕСКИЙ
Время: 15 минут
```

**Файл:** `.env` или Settings → Environment

```bash
# Google Sheets
GOOGLE_SHEET_ID=<your-sheet-id>

# Telegram
TELEGRAM_CHAT_ID=<ваш chat ID>
TELEGRAM_BOT_TOKEN=<токен от @BotFather>

# OpenRouter (если используется)
OPENROUTER_REFERER=https://n8n.io
```

---

#### 1.3 Telegram Bot Setup
```bash
Приоритет: 🔴 КРИТИЧЕСКИЙ
Время: 30 минут
```

**Действия:**
1. Создать бота через @BotFather
2. Получить token
3. Добавить в WF 0:
   - Telegram Trigger credential
   - Telegram Send Message credential
4. Настроить webhook для production

**Результат:** Telegram Bot готов принимать команды

---

### Этап 2: Исправление AI Orchestrator (Критически важно)

#### 2.1 Добавить executeWorkflowTrigger во все вызываемые workflows
```bash
Приоритет: 🔴 КРИТИЧЕСКИЙ
Время: 2 часа
```

**Для каждого workflow (1-12):**

1. Добавить node `Execute Workflow Trigger`
2. Определить `workflowInputs`:

**Пример для TikTok Parser (WF 1):**
```json
{
  "workflowInputs": {
    "values": [
      {"name": "username", "type": "string"},
      {"name": "chat_id", "type": "string"}
    ]
  }
}
```

**Список inputs для каждого:**

| Workflow | Required Inputs |
|----------|----------------|
| WF 1 - TikTok | username, chat_id |
| WF 2 - Instagram | username, chat_id |
| WF 4 - YouTube | channel_url, chat_id |
| WF 7 - YouTube Deep | channel_url, chat_id |
| WF 8 - Pinterest | username, chat_id |
| WF 5 - Carousel | topic, chat_id |
| WF 3 - Video Montage | content_ids (array), chat_id |
| WF 6 - Threads | content_type, content_data, chat_id |
| WF 9 - Stats | period, chat_id |
| WF 10 - Analytics | period, chat_id |
| WF 11 - Vector | query, chat_id |
| WF 12 - Pinterest Pub | content_data, chat_id |

---

#### 2.2 Обновить tool definitions в AI Orchestrator
```bash
Приоритет: 🔴 КРИТИЧЕСКИЙ
Время: 1 час
```

**Для каждого tool добавить workflowInputs:**

```json
{
  "name": "add_tiktok_competitor",
  "description": "Add TikTok account to monitoring. Input: username (without @)",
  "workflowId": "fqUk7t9De8NXzQqf",
  "workflowInputs": {
    "mappingMode": "defineBelow",
    "value": {
      "username": "={{ $fromAI('username', '', 'string') }}",
      "chat_id": "={{ $('Telegram Trigger').item.json.message.chat.id }}"
    }
  }
}
```

**Результат:** AI агент может вызывать workflows с параметрами

---

### Этап 3: Credentials Setup

#### 3.1 Обязательные (для парсеров)
```bash
Приоритет: 🟠 ВЫСОКИЙ
Время: 30 минут
```

- ✅ ScraperCreators API (уже есть?)
- ✅ Google Sheets OAuth2 (уже есть?)
- ✅ OpenAI API (уже есть?)
- ✅ Anthropic API (нужен ключ)
- ✅ Supabase API (нужен ключ)

#### 3.2 Опциональные (для генерации)
```bash
Приоритет: 🟡 СРЕДНИЙ
Время: 1 час
```

- ⏳ HeyGen API (для Video Montage)
- ⏳ Ideogram API (для Carousel/Video)
- ⏳ Threads API (для публикации)
- ⏳ Pinterest API (для публикации)

**Альтернатива:** Отключить workflows генерации/публикации до получения ключей

---

### Этап 4: Supabase Vector Store Setup
```bash
Приоритет: 🟡 СРЕДНИЙ
Время: 1 час
```

**SQL для создания таблицы:**
```sql
CREATE TABLE documents (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  content TEXT NOT NULL,
  metadata JSONB,
  embedding VECTOR(1536)
);

CREATE INDEX ON documents USING ivfflat (embedding vector_cosine_ops);

-- Function for vector search
CREATE OR REPLACE FUNCTION match_documents(
  query_embedding VECTOR(1536),
  match_threshold FLOAT DEFAULT 0.7,
  match_count INT DEFAULT 10
)
RETURNS TABLE (
  id UUID,
  content TEXT,
  metadata JSONB,
  similarity FLOAT
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT
    documents.id,
    documents.content,
    documents.metadata,
    1 - (documents.embedding <=> query_embedding) AS similarity
  FROM documents
  WHERE 1 - (documents.embedding <=> query_embedding) > match_threshold
  ORDER BY similarity DESC
  LIMIT match_count;
END;
$$;
```

**Результат:** Vector search готов к работе

---

### Этап 5: Первое тестирование

#### 5.1 Тест парсеров
```bash
Приоритет: 🟠 ВЫСОКИЙ
Время: 2 часа
```

**Действия:**
1. Добавить 2-3 конкурента в лист `Competitors`
2. Активировать WF 1 (TikTok) - запустить вручную
3. Проверить:
   - ✅ Данные появились в `Parsed_Videos`
   - ✅ Расходы записались в `API_Costs`
   - ✅ Уведомление пришло в Telegram
4. Повторить для WF 2 (Instagram)

**Ожидаемый результат:** Парсеры работают

---

#### 5.2 Тест AI Orchestrator
```bash
Приоритет: 🔴 КРИТИЧЕСКИЙ
Время: 1 час
```

**Команды для теста:**
1. "добавь конкурента viral из тиктока"
2. "статистика за неделю"
3. "найди похожие примеры про AI"

**Проверить:**
- ✅ AI агент вызывает правильный tool
- ✅ Параметры передаются корректно
- ✅ Workflow выполняется
- ✅ Ответ приходит в Telegram

**Результат:** Оркестрация работает

---

### Этап 6: Доработка функциональности

#### 6.1 Механизм выбора топ контента
```bash
Приоритет: 🟡 СРЕДНИЙ
Время: 2 часа
```

**Новый workflow или code node:**
```javascript
// Выбрать топ 5 видео за последнюю неделю
// с virality = VIRUS и отсортировать по views
SELECT * FROM Parsed_Videos
WHERE virality = 'VIRUS'
  AND parsed_at > NOW() - INTERVAL '7 days'
ORDER BY views DESC
LIMIT 5
```

Вернуть `content_ids` для Video Montage

---

#### 6.2 Унификация форматов
```bash
Приоритет: 🟡 СРЕДНИЙ
Время: 1 час
```

**Изменить парсеры:**
- VFR и ER конвертировать в **number**
- Добавить обработчик отсутствующего `chat_id`

**Code node в каждом парсере:**
```javascript
return {
  ...item.json,
  vfr: parseFloat(item.json.vfr),
  er: parseFloat(item.json.er),
  share_rate: parseFloat(item.json.share_rate),
  chat_id: item.json.chat_id || $env.TELEGRAM_CHAT_ID
};
```

---

### Этап 7: Мониторинг и надёжность

#### 7.1 Error handling
```bash
Приоритет: 🟢 НИЗКИЙ
Время: 2 часа
```

- Добавить Try-Catch nodes в критических местах
- Настроить retry логику (3 попытки с задержкой)
- Отправлять алерты в Telegram при ошибках

#### 7.2 Health checks
```bash
Приоритет: 🟢 НИЗКИЙ
Время: 1 час
```

- Проверка доступности API раз в 5 минут
- Логирование всех выполнений
- Dashboard с метриками

---

## 🎯 ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

### 🔴 Критические (БЕЗ ЭТОГО СИСТЕМА НЕ РАБОТАЕТ)

1. ✅ Создать Google Sheets со структурой
2. ✅ Настроить Environment Variables
3. ✅ Настроить Telegram Bot
4. ✅ Добавить `executeWorkflowTrigger` во все workflows
5. ✅ Обновить tool definitions в AI Orchestrator
6. ✅ Получить недостающие API credentials

**Время:** ~6-8 часов работы
**Без этого:** Система вообще не запустится

---

### 🟠 Высокие (СИСТЕМА РАБОТАЕТ, НО С ОГРАНИЧЕНИЯМИ)

1. Протестировать парсеры
2. Протестировать AI Orchestrator
3. Создать Supabase таблицу
4. Добавить механизм выбора топ контента

**Время:** ~4-6 часов
**Без этого:** Нет генерации контента и vector search

---

### 🟡 Средние (УЛУЧШЕНИЯ)

1. Унифицировать форматы данных
2. Настроить публикаторы (Threads, Pinterest)
3. Доработать Video Montage
4. Оптимизировать промпты AI

**Время:** ~6-8 часов
**Без этого:** Нет автопубликации

---

### 🟢 Низкие (ОПТИМИЗАЦИЯ)

1. Error handling
2. Health checks
3. Мониторинг
4. Dashboard

**Время:** ~4-6 часов
**Без этого:** Система работает, но без надёжности

---

## 📊 ИТОГОВАЯ ОЦЕНКА

| Категория | Статус | Готовность |
|-----------|--------|------------|
| **Архитектура** | ❌ Критические ошибки | 30% |
| **Инфраструктура** | ❌ Не настроена | 0% |
| **Парсеры** | ⚠️ Готовы, но не настроены | 70% |
| **Генераторы** | ❌ Не работают | 20% |
| **Публикаторы** | ❌ Не настроены | 10% |
| **AI Orchestrator** | ❌ Не работает | 40% |
| **Аналитика** | ⚠️ Частично готова | 50% |
| **Мониторинг** | ❌ Отсутствует | 0% |

**Общая готовность системы:** **~30%**

---

## 🚀 БЫСТРЫЙ СТАРТ (Минимальная рабочая версия)

### Цель: Запустить только парсеры

**Время:** 2-3 часа

**Действия:**
1. ✅ Создать Google Sheets
2. ✅ Настроить $env.GOOGLE_SHEET_ID
3. ✅ Добавить 3-5 конкурентов
4. ✅ Активировать WF 1, 2
5. ✅ Дождаться первого парсинга

**Результат:**
- Парсеры работают по расписанию
- Данные собираются в Google Sheets
- Можно анализировать вручную

**НЕ РАБОТАЕТ:**
- AI Orchestrator (команды через Telegram)
- Генерация контента
- Публикация
- Vector search

---

## 📝 ЧЕКЛИСТ НАСТРОЙКИ

### Инфраструктура
- [ ] Google Sheets создан и настроен
- [ ] Environment variables определены
- [ ] Telegram Bot создан и подключён
- [ ] Supabase таблица создана

### Credentials
- [ ] ScraperCreators API
- [ ] Google Sheets OAuth2
- [ ] OpenAI API
- [ ] Anthropic API (Claude)
- [ ] Supabase API
- [ ] Telegram Bot Token
- [ ] HeyGen API (опционально)
- [ ] Ideogram API (опционально)
- [ ] Threads API (опционально)
- [ ] Pinterest API (опционально)

### Workflows
- [ ] WF 1 - executeWorkflowTrigger добавлен
- [ ] WF 2 - executeWorkflowTrigger добавлен
- [ ] WF 3 - executeWorkflowTrigger добавлен
- [ ] WF 4 - executeWorkflowTrigger добавлен
- [ ] WF 5 - executeWorkflowTrigger добавлен
- [ ] WF 6 - executeWorkflowTrigger добавлен
- [ ] WF 7 - executeWorkflowTrigger добавлен
- [ ] WF 8 - executeWorkflowTrigger добавлен
- [ ] WF 9 - executeWorkflowTrigger добавлен
- [ ] WF 10 - executeWorkflowTrigger добавлен
- [ ] WF 11 - executeWorkflowTrigger добавлен
- [ ] WF 12 - executeWorkflowTrigger добавлен
- [ ] WF 0 - tool definitions обновлены

### Тестирование
- [ ] Парсер TikTok работает
- [ ] Парсер Instagram работает
- [ ] AI Orchestrator принимает команды
- [ ] Vector search работает
- [ ] Генерация контента работает
- [ ] Публикация работает

---

**Следующий шаг:** Начать с Этапа 1 - Базовая инфраструктура
