# 📊 Google Sheets Setup - Content Factory

**Дата:** 2026-01-15
**Цель:** Создать Google Sheet с правильной структурой для всей системы

---

## 📋 Общая информация

### Что создаём:
- **1 файл Google Sheets** с **4 листами (tabs)**
- Каждый лист = отдельная таблица данных
- Первая строка каждого листа = заголовки столбцов

### Название файла:
```
Content Factory - Data
```

---

## 🗂️ Структура листов

### Лист 1: **Competitors**

Хранит список отслеживаемых конкурентов

**Заголовки (первая строка):**
```
id	username	platform	status	last_parsed	followers	added_date
```

**Типы данных:**
- `id` - число (авто-инкремент, руками вписываешь)
- `username` - текст (без @)
- `platform` - текст (tiktok / instagram / youtube / pinterest)
- `status` - текст (active / paused / deleted)
- `last_parsed` - дата-время (2026-01-15 12:00:00)
- `followers` - число
- `added_date` - дата (2026-01-15)

**Пример данных (строка 2):**
```
1	viral_guy	tiktok	active	2026-01-15 12:00:00	125000	2026-01-15
```

**Пример данных (строка 3):**
```
2	trendy_girl	instagram	active	2026-01-15 13:00:00	89000	2026-01-15
```

---

### Лист 2: **Parsed_Videos**

Хранит все спарсенные видео с метриками виральности

**Заголовки (первая строка):**
```
id	competitor_id	competitor_username	platform	url	views	likes	comments	shares	vfr	er	share_rate	virality	hook	description	hashtags	duration	thumbnail_url	created_at	parsed_at	chat_id
```

**Типы данных:**
- `id` - текст (уникальный ID видео, например "7123456789012345678")
- `competitor_id` - число (связь с листом Competitors)
- `competitor_username` - текст
- `platform` - текст (tiktok / instagram / youtube / pinterest)
- `url` - текст (полная ссылка)
- `views` - число
- `likes` - число
- `comments` - число
- `shares` - число
- `vfr` - число с десятичными (Views/Followers Ratio, например 1.23)
- `er` - число с десятичными (Engagement Rate %, например 4.56)
- `share_rate` - число с десятичными (Share Rate %, например 0.12)
- `virality` - текст (VIRUS / AVERAGE / LOW)
- `hook` - текст (первая строка описания)
- `description` - текст (полное описание)
- `hashtags` - текст (#tag1, #tag2, #tag3)
- `duration` - число (секунды)
- `thumbnail_url` - текст (ссылка на превью)
- `created_at` - дата-время (когда видео опубликовано)
- `parsed_at` - дата-время (когда спарсено)
- `chat_id` - текст (Telegram chat ID, если через бота)

**Пример данных (строка 2):**
```
7123456789012345678	1	viral_guy	tiktok	https://tiktok.com/@viral_guy/video/7123456789012345678	1500000	75000	5000	2500	12.0	5.5	0.17	VIRUS	This is the hook text	Full video description here with all details #viral #trending	#viral, #trending, #fyp	30	https://p16-sign.tiktokcdn.com/xxx	2026-01-10 18:30:00	2026-01-15 12:05:00	123456789
```

**Критично для формул:**
- `vfr`, `er`, `share_rate` должны быть **ЧИСЛА**, не текст!
- Можно использовать формулы для расчёта, если парсер не считает

---

### Лист 3: **API_Costs**

Трекинг расходов на все API

**Заголовки (первая строка):**
```
timestamp	service	operation	cost	details
```

**Типы данных:**
- `timestamp` - дата-время (2026-01-15 12:00:00)
- `service` - текст (scraper_creators / openai / anthropic / heygen / ideogram)
- `operation` - текст (tiktok_parse / instagram_parse / analysis / video_generation)
- `cost` - число с десятичными (в долларах, например 0.03)
- `details` - текст (JSON с деталями, например {"username": "viral", "videos": 30})

**Пример данных (строка 2):**
```
2026-01-15 12:00:00	scraper_creators	tiktok_parse	0.03	{"username": "viral_guy", "videos": 30}
```

**Пример данных (строка 3):**
```
2026-01-15 12:05:00	openai	embeddings	0.002	{"model": "text-embedding-3-small", "tokens": 2000}
```

**Пример данных (строка 4):**
```
2026-01-15 12:10:00	anthropic	chat_completion	0.015	{"model": "claude-3-5-sonnet", "input_tokens": 1500, "output_tokens": 500}
```

---

### Лист 4: **Published_Content**

Контент опубликованный системой (Threads, Pinterest)

**Заголовки (первая строка):**
```
id	platform	content_type	url	published_at	views	likes	comments	shares	performance_score	source_video_id
```

**Типы данных:**
- `id` - текст (уникальный ID поста)
- `platform` - текст (threads / pinterest)
- `content_type` - текст (video / carousel / image)
- `url` - текст (ссылка на опубликованный контент)
- `published_at` - дата-время
- `views` - число
- `likes` - число
- `comments` - число
- `shares` - число
- `performance_score` - число с десятичными (расчётная метрика)
- `source_video_id` - текст (связь с Parsed_Videos, если контент создан на основе видео)

**Пример данных (строка 2):**
```
threads_123456	threads	carousel	https://threads.net/@myaccount/post/123456	2026-01-15 14:00:00	5000	250	30	10	8.5	7123456789012345678
```

---

## 🔧 Инструкция по созданию

### Шаг 1: Создать Google Sheet
1. Открой [Google Sheets](https://sheets.google.com)
2. Нажми "Создать новую таблицу"
3. Назови: **Content Factory - Data**

### Шаг 2: Переименовать первый лист
1. Снизу кликни правой кнопкой на "Лист1"
2. "Переименовать"
3. Назови: **Competitors**

### Шаг 3: Создать структуру листа Competitors
1. В ячейку A1 вставь: `id`
2. В B1: `username`
3. В C1: `platform`
4. В D1: `status`
5. В E1: `last_parsed`
6. В F1: `followers`
7. В G1: `added_date`

**ИЛИ** скопируй всю строку целиком:
```
id	username	platform	status	last_parsed	followers	added_date
```
И вставь в A1 (Tab между значениями автоматически разнесёт по ячейкам)

### Шаг 4: Добавить пример данных
Строка 2:
```
1	viral_guy	tiktok	active	2026-01-15 12:00:00	125000	2026-01-15
```

Строка 3:
```
2	trendy_girl	instagram	active	2026-01-15 13:00:00	89000	2026-01-15
```

### Шаг 5: Создать остальные листы
1. Нажми "+" внизу листов
2. Создай лист **Parsed_Videos**
3. Вставь заголовки из раздела выше
4. Повтори для **API_Costs** и **Published_Content**

### Шаг 6: Настроить форматирование (опционально)
- Первая строка каждого листа - **жирный шрифт**
- Закрепить первую строку: View → Freeze → 1 row
- Для столбцов с числами: Format → Number → Number (2 decimal places)
- Для дат: Format → Number → Date time

### Шаг 7: Получить ID таблицы
1. Скопируй URL таблицы
2. ID это часть между `/d/` и `/edit`:
```
https://docs.google.com/spreadsheets/d/[ЭТО_ТВОЙ_ID]/edit
```

Например:
```
https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit
                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                                       ЭТО ID
```

### Шаг 8: Настроить доступ
1. Нажми "Поделиться" (Share) справа вверху
2. "Anyone with the link can **edit**" (важно! n8n должен записывать)
3. ИЛИ добавь конкретный Google аккаунт с правами Editor

---

## 📋 Готовый шаблон для копирования

### Весь лист Competitors:
```
id	username	platform	status	last_parsed	followers	added_date
1	viral_guy	tiktok	active	2026-01-15 12:00:00	125000	2026-01-15
2	trendy_girl	instagram	active	2026-01-15 13:00:00	89000	2026-01-15
```

### Весь лист Parsed_Videos:
```
id	competitor_id	competitor_username	platform	url	views	likes	comments	shares	vfr	er	share_rate	virality	hook	description	hashtags	duration	thumbnail_url	created_at	parsed_at	chat_id
```

### Весь лист API_Costs:
```
timestamp	service	operation	cost	details
2026-01-15 12:00:00	scraper_creators	tiktok_parse	0.03	{"username": "viral_guy", "videos": 30}
2026-01-15 12:05:00	openai	embeddings	0.002	{"model": "text-embedding-3-small", "tokens": 2000}
```

### Весь лист Published_Content:
```
id	platform	content_type	url	published_at	views	likes	comments	shares	performance_score	source_video_id
```

---

## ✅ Чек-лист после создания

- [ ] Создана таблица "Content Factory - Data"
- [ ] 4 листа созданы и переименованы
- [ ] Все заголовки на месте
- [ ] Добавлены примеры данных для проверки
- [ ] Первая строка закреплена на всех листах
- [ ] Настроены права доступа (Edit для всех по ссылке)
- [ ] **Скопирован ID таблицы**
- [ ] ID добавлен в n8n Environment Variables как `GOOGLE_SHEET_ID`

---

## 🔗 Следующий шаг

После создания таблицы:
1. Скопируй **ID таблицы**
2. Добавь в n8n: Settings → Variables → Environment
3. Ключ: `GOOGLE_SHEET_ID`
4. Значение: твой ID

**Готово!** 🎉

---

**Примечание:** Эта таблица будет использоваться всеми 14 workflows для чтения и записи данных.
